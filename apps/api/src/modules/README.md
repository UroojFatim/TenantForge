# Modules

Feature modules should live here when the domain grows.

Keep each future module small and self-contained so auth, tenancy, and MongoDB integration can be introduced without flattening the API structure.