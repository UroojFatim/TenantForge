# TenantForge — Frontend

This workspace already contains a Vite-based frontend at the project root.

Quick notes:
- The existing frontend source is in `src/` at the project root.
- To run the frontend (from the project root):

```bash
npm install
npm run dev
```

If you want I can move the frontend files into this `frontend/` folder so the project becomes a monorepo with `frontend/` and `backend/` directories — tell me if you'd like that.
