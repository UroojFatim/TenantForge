{
  "name": "@bizpilothub/utils",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean --out-dir dist",
    "lint": "eslint .",
    "test": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@bizpilothub/eslint-config": "0.1.0",
    "@bizpilothub/tsconfig": "0.1.0",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3"
  }
}